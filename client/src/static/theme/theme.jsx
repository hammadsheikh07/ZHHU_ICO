import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ffff00",
    },
  },
  typography: {
    fontFamily: "Nunito",
  },
});

export default theme;
